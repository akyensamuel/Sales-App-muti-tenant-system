# .env.example - Template for Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to version control

SECRET_KEY=your-django-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,[::1]

# =============================================================================
# DATABASE CONFIGURATION - CHOOSE ONE BASED ON DEVELOPMENT ENVIRONMENT
# =============================================================================

# 🔄 SWITCH BETWEEN ENVIRONMENTS BY COMMENTING/UNCOMMENTING:

# 🌐 PRODUCTION/ONLINE DEVELOPMENT - Supabase PostgreSQL (Main Database)
# Use this when working with production data or testing online integrations
DATABASE_URL=postgresql://your-supabase-user:your-password@your-host:port/your-database

# 💻 LOCAL DEVELOPMENT - SQLite (Fast Local Testing)  
# Use this for quick local development and testing
# DATABASE_URL=sqlite:///db.sqlite3

# =============================================================================
# TENANT DATABASE EXAMPLES (Reference Only - Used in Commands)
# =============================================================================

# 🏢 EXTERNAL POSTGRESQL FOR TENANTS (Example: Render.com, Heroku, etc.)
# TENANT_DB_URL=postgresql://username:password@host:port/database

# 💾 LOCAL SQLITE FOR TENANT TESTING
# TENANT_DB_URL=sqlite:///tenant_dbs/tenant_name.sqlite3

# =============================================================================
# DEVELOPMENT WORKFLOW INSTRUCTIONS
# =============================================================================
# 
# SETUP STEPS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual SECRET_KEY and database credentials
# 3. Choose your database configuration (see below)
#
# FOR LOCAL DEVELOPMENT:
# 1. Comment out the PostgreSQL DATABASE_URL line (add # at start)
# 2. Uncomment the SQLite DATABASE_URL line (remove # at start)  
# 3. Run: python manage.py migrate
# 4. Create tenants: python manage.py create_tenant "Local Test" "local" "admin@local.com"
#
# FOR PRODUCTION/ONLINE TESTING:
# 1. Uncomment the PostgreSQL DATABASE_URL line (remove # at start)
# 2. Comment out the SQLite DATABASE_URL line (add # at start)
# 3. Run: python manage.py migrate  
# 4. Create tenants with external DB: 
#    python manage.py create_tenant "Prod Test" "prod" "admin@prod.com" --database-url="postgresql://..."
#
# SECURITY NOTES:
# - Never commit .env to version control
# - Use strong passwords for production databases
# - Change default user credentials immediately after tenant creation
# - Keep database URLs private and secure
# =============================================================================
